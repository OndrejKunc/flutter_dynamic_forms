name: example
description: example
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  dynamic_forms: ^0.5.0

dev_dependencies:
  build_runner: ^1.0.0
  dynamic_forms_generator:
    path: ../
